/* ============================================================
   STATE.JS — Поточні дані сторінки (Search / Main)
   ============================================================ */

let allData             = [];
let currentFilteredData = [];
let currentPage         = 1;
let isRegisterMode      = false;
let currentSort         = 'new';
const itemsPerPage      = 16;
